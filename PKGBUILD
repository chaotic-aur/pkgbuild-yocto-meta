# Maintainer: dr460nf1r3 <dr460nf1r3 at garudalinux dot org>

pkgname=yocto-support
pkgver=1
pkgrel=1
arch=('any')
url="https://dr460nf1r3.me"
license=('GPL')
pkgdesc="Meta package for building Yocto images"
depends=(bmap-tools
        chrpath
        cpio
        debianutils
        diffstat
        gawk
        gcc
        git
        iputils
        python-beautifulsoup4
        python-codegen
        python-django
        python-gitpython
        python-jinja
        python-pexpect
        python-pip
        python-ply
        python-progressbar
        python-pyinotify
        python-pylint
        python-simplediff
        python-subunit
        rpcsvc-proto
        socat
        texinfo
        unzip
        wget
        xterm)
